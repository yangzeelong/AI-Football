# TODO：单视角质量问题跟进

来源报告：`reports\C1_quality_report.md`

视频：`data\素材\射门1-1080p60.mov`

运行信息：C1，1920x1080，60fps，stride=1，7201 帧

当前基线：

| 指标 | 数值 | 说明 |
| --- | ---: | --- |
| total score | 64.6 | 无标注粗评分，只用于横向比较参数/模型 |
| keypoint | 60.64 | 关键点平均置信度中等 |
| person tracking | 61.59 | 人体轨迹碎片偏多 |
| ball tracking | 61.52 | 足球缺失偏多 |
| stability | 83.99 | 当前抖动不是主要短板 |

## P0：足球检测与跟踪连续性

- [x] 复查报告列出的 `no_ball` 片段，已确认主要原因是足球检测漏检。
- [ ] 重点查看 `4.57s`、`11.15s`、`14.20s` 附近的原视频和回放结果，整理典型漏检样例。
- [ ] 调低足球检测过滤阈值，先尝试 `ball.min_confidence=0.10`。
- [ ] 调低足球最小框尺寸，先尝试 `ball.min_width_px=2`、`ball.min_height_px=2`。
- [ ] 对比阈值调整前后的 `ball present ratio`、`max missing streak` 和误检数量。
- [ ] 如果 COCO `sports ball` 仍然不稳定，评估是否需要足球专用检测模型或少量足球数据微调。

验收目标：

- `ball present ratio` 明显高于当前 `0.6152`。
- `max missing streak` 明显低于当前 `665` 帧。
- 在控制误检数量的前提下，减少足球检测漏检。

## P1：人体单镜头跟踪碎片

- [ ] 抽查短轨迹对应的视频片段，确认短轨迹来自误检、遮挡、检测框抖动还是 ID 切换。
- [ ] 调整人体检测阈值和最小框过滤规则，观察短轨迹数量变化。
- [ ] 调整 BoT-SORT 参数，重点观察遮挡后是否能恢复原 ID。
- [ ] 在报告中补充更直接的 ID switch / track fragment 指标。

当前问题证据：

| 指标 | 当前值 |
| --- | ---: |
| total tracks | 164 |
| avg track length | 154.366 帧 |
| short track count | 63 |
| short track ratio | 0.3841 |

验收目标：

- `short track ratio` 低于当前 `0.3841`。
- 平均 track 长度提升。
- 遮挡后 ID 恢复更稳定。

## P1：脚部和手腕关键点质量

- [ ] 人工抽查最差关键点，确认是低置信度但位置可用，还是位置明显错误。
- [ ] 重点查看脚尖、脚跟、手腕、手肘点位。
- [ ] 给低置信度关键点增加更清晰的状态标记，避免被业务逻辑当作高质量观测。
- [ ] 后续动作分析如果依赖支撑脚、触球脚或摆腿，需要单独建立脚部点位验收样例。

当前最弱关键点：

| keypoint | avg confidence | low confidence ratio |
| --- | ---: | ---: |
| right_big_toe | 0.4952 | 0.1196 |
| right_small_toe | 0.5056 | 0.1064 |
| right_wrist | 0.5128 | 0.1354 |
| left_big_toe | 0.5145 | 0.1098 |
| left_wrist | 0.5229 | 0.1512 |

验收目标：

- 低置信度点不会被误用为可靠动作依据。
- 脚部关键点在射门、带球、单脚跳场景中有人工抽查结论。

## P2：质量评估脚本增强

- [ ] 报告中增加连续 `no_ball` 片段区间，而不是只列前 N 个单帧。
- [ ] 增加按时间段统计的球缺失比例，便于定位模型失效片段。
- [ ] 增加 track fragment / ID switch 相关指标。
- [ ] 增加可选截图导出，自动保存最需要复查的帧。

## 下一步建议

优先处理足球连续性，再处理人体跟踪碎片。关键点当前先做人工抽查和状态标记，不建议马上大改模型链路。
